// import { useToast } from "@/components/ui/use-toast";  // Assuming you've created a custom use-toast
// import { ToastActionElement, ToastProps } from "@/components/ui/toast";  // Adjust as per your actual setup

// export function Toaster() {
//   const { toasts } = useToast();

//   return (
//     <div className="toast-container">
//       {toasts.map((toast) => (
//         <div key={toast.id} className="toast">
//           <h4>{toast.title}</h4>
//           <p>{toast.description}</p>
//           <button onClick={toast.dismiss}>Dismiss</button>
//         </div>
//       ))}
//     </div>
//   );
// }
